<Page x:Class="QQ112.Pages.NewUser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QQ112.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NewUser">

    <Grid>
        <StackPanel>
            <TextBox Text="{Binding NewUserss.ID_users}"  Width="300" Margin="0,140,0,10"/>
            <TextBox Text="{Binding NewUserss.login}" Width="300" Margin="0,0,0,10"/>
            <TextBox  Text="{Binding NewUserss.password}" Width="300" Margin="0,0,0,10"/>
            <TextBox  Text="{Binding NewUserss.FIO}" Width="300" Margin="0,0,0,10"/>
            <ComboBox Text="{Binding NewUserss.ID_Role}" Width="300" Margin="0,0,0,10">
                <ComboBoxItem Content="1"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="3"/>
            </ComboBox>
            <Button Width="120" Height="20" Click="Button_Click" />
        </StackPanel>
    </Grid>
</Page>
